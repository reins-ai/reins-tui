// /setup is an alias for /onboard — all logic lives in onboard.ts.
export { handleOnboardCommand as handleSetupCommand, resetOnboarding } from "./onboard";
export type { ResetOnboardingDeps } from "./onboard";
